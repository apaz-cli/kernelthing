# Incomplete Tasks Detected

You are trying to finish, but your `todowrite` list still has **incomplete
`[mainline]`/`[blocking]` tasks** (untagged tasks count as blocking):

{{INCOMPLETE_LIST}}

**Required Action**:
1. Finish each task above, then call `todowrite` to set its `status` to `completed`.
2. If a task is genuinely non-blocking follow-up, re-tag its content with a
   leading `[queued]` instead -- queued items do not block finishing.
3. Only once no `[mainline]`/`[blocking]` task remains open may you write your
   summary and finish.

Do NOT proceed to the reviewer until the blocking tasks are resolved. This saves time and ensures thorough work.
